#include "main.h"
	/**
 * main - check the code
 *
 * Return: Always 0.
 */
	void print_alphabet(void)
/* this is alphabet function*/
{
		char letter;

		for (letter = 'a'; letter <= 'z'; letter++)
			_putchar(letter);
		_putchar('\n');
}
